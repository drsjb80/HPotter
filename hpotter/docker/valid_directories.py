# Keep valid directories from docker here
# Write block that executes some commands, then appends each directory to a list.
# Save lists and user for comparisons, detecting wrong directories input by the user
