SELECT
  c.username,
  COUNT(*) AS attempts
FROM credentials c
LEFT JOIN connections conn ON c.connections_id = conn.id
WHERE c.username IS NOT NULL
  AND c.username != ''
  AND $__timeFilter(conn.created_at)
GROUP BY c.username
ORDER BY attempts DESC
LIMIT 10
